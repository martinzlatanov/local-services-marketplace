---
phase: 08-mobile-client-job-discovery-acceptance
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/mobile/app/(app)/jobs/[id].tsx
  - apps/mobile/app/(app)/(tabs)/settings.tsx
autonomous: true
requirements:
  - DISC-03
must_haves:
  truths:
    - "Provider can view full job details before accepting"
    - "Single tap Accept calls the API with version and handles 409 conflicts"
    - "Provider can edit service area from Settings"
  artifacts:
    - path: "apps/mobile/app/(app)/jobs/[id].tsx"
      provides: "Job detail with Accept button"
    - path: "apps/mobile/app/(app)/(tabs)/settings.tsx"
      provides: "Settings with edit area and logout"
  key_links:
    - from: "apps/mobile/app/(app)/jobs/[id].tsx"
      to: "/api/jobs/:id/accept"
      via: "acceptJob(token, id, version)"
      pattern: "accept"
---

<objective>
Add the job detail and settings screens so providers can inspect job details, accept with a single tap, and edit their service area.

Purpose: Deliver the detail/accept flow and service area editing per DISC-03 and Phase 8 UX decisions.
Output: Job detail screen and Settings tab.
</objective>

<execution_context>
@/Users/mzlatanov/Desktop/VS Code Projects/local-services-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mzlatanov/Desktop/VS Code Projects/local-services-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mobile-client-job-discovery-acceptance/08-CONTEXT.md
@.planning/phases/08-mobile-client-job-discovery-acceptance/08-UI-SPEC.md
@apps/mobile/lib/api.ts
@apps/mobile/hooks/useServiceArea.ts
@apps/mobile/contexts/AuthContext.tsx

<interfaces>
From apps/mobile/lib/api.ts:
```typescript
export async function getJob(token: string, id: string): Promise<JobDto>
export async function acceptJob(token: string, id: string, version: number): Promise<JobDto>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build job detail screen with single-tap accept + 409 handling</name>
  <read_first>
    apps/mobile/lib/api.ts
    .planning/phases/08-mobile-client-job-discovery-acceptance/08-UI-SPEC.md
  </read_first>
  <files>apps/mobile/app/(app)/jobs/[id].tsx</files>
  <action>
    - Implement job detail screen per UI-SPEC:
      - Appbar with BackAction and title "Job details".
      - Fetch job via `getJob` on mount using `id` param and auth token.
      - Render heading (category) and labeled fields for Description, Timeframe, Area.
      - Accept button is contained, full width, and uses `acceptJob(token, id, job.version)`.
      - On 200: `router.back()`.
      - On 409: show Snackbar "Job already taken" and navigate back to feed with a param (e.g., `router.replace({ pathname: '/(app)/(tabs)/feed', params: { conflict: '1' } })`).
      - On other errors: show Snackbar "Couldn't accept job. Please try again." and keep the user on the detail screen.
      - While loading, show ActivityIndicator; on fetch failure, show the error banner copy from UI-SPEC and hide the Accept button.
  </action>
  <acceptance_criteria>
    - Detail screen renders category, description, timeframe, and area labels per UI-SPEC.
    - Accept button calls `acceptJob` with the job version.
    - 409 path shows "Job already taken" and returns to feed.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck --workspace=apps/mobile</automated>
  </verify>
  <done>Detail screen supports single-tap acceptance and proper conflict handling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Settings tab with area edit and logout</name>
  <read_first>
    apps/mobile/hooks/useServiceArea.ts
    apps/mobile/contexts/AuthContext.tsx
    .planning/phases/08-mobile-client-job-discovery-acceptance/08-UI-SPEC.md
  </read_first>
  <files>apps/mobile/app/(app)/(tabs)/settings.tsx</files>
  <action>
    - Implement Settings tab per UI-SPEC:
      - Appbar title "Settings".
      - Service Area section with current area and a dialog to change area using `CITY_AREAS` and `saveServiceArea`.
      - Account section shows user email (disabled List.Item).
      - Log out button calls `logout()` and uses `theme.colors.error` for text.
    - When area is updated, close the dialog and rely on Feed focus to refetch jobs.
  </action>
  <acceptance_criteria>
    - Settings screen shows current area and opens a dialog to update it.
    - Logout button calls `logout()` and uses error color text.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck --workspace=apps/mobile</automated>
  </verify>
  <done>Settings allows editing the service area and logging out.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace=apps/mobile`
</verification>

<success_criteria>
- Detail screen shows full job details and accepts with version-based API call.
- Settings screen supports editing the service area and logout.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-client-job-discovery-acceptance/08-03-SUMMARY.md`
</output>
