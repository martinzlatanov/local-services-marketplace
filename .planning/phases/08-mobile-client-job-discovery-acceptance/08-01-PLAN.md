---
phase: 08-mobile-client-job-discovery-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/index.ts
  - apps/mobile/contexts/AuthContext.tsx
  - apps/mobile/hooks/useServiceArea.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/index.tsx
  - apps/mobile/app/(app)/_layout.tsx
  - apps/mobile/app/(app)/(tabs)/_layout.tsx
  - apps/mobile/app/(app)/onboarding.tsx
autonomous: true
requirements:
  - DISC-01
must_haves:
  truths:
    - "Provider is forced to pick a service area before accessing the feed"
    - "Service area is stored on device and reused on next launch"
  artifacts:
    - path: "packages/types/src/index.ts"
      provides: "CITY_AREAS constant exported as readonly string[]"
      contains: "export const CITY_AREAS"
    - path: "apps/mobile/hooks/useServiceArea.ts"
      provides: "SecureStore read/write helpers for service_area"
    - path: "apps/mobile/app/(app)/onboarding.tsx"
      provides: "Service area picker with Save area CTA"
    - path: "apps/mobile/app/(app)/(tabs)/_layout.tsx"
      provides: "Bottom tabs for Feed and Settings"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/app/(app)/onboarding.tsx"
      via: "NavigationGuard redirect when service_area missing"
      pattern: "service_area"
---

<objective>
Define the shared city/area options, add SecureStore-backed service area handling, and scaffold the mobile app routing with a forced onboarding gate and tabs.

Purpose: Establish the service-area contract and navigation structure required for job discovery.
Output: City area constant, service-area hook, onboarding screen, and tab-based app shell.
</objective>

<execution_context>
@/Users/mzlatanov/Desktop/VS Code Projects/local-services-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mzlatanov/Desktop/VS Code Projects/local-services-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-mobile-client-job-discovery-acceptance/08-CONTEXT.md
@.planning/phases/08-mobile-client-job-discovery-acceptance/08-UI-SPEC.md
@packages/types/src/index.ts
@apps/mobile/contexts/AuthContext.tsx
@apps/mobile/app/_layout.tsx

<interfaces>
From packages/types/src/index.ts:
```typescript
export enum JobStatus {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  IN_PROGRESS = 'IN_PROGRESS',
  COMPLETED = 'COMPLETED',
}

export interface JobDto {
  id: string
  status: JobStatus
  version: number
  category: string
  description: string
  timeframe: string
  cityArea: string
  clientId: string
  providerId: string | null
  createdAt: string
  updatedAt: string
}

export interface AuthUserDto {
  id: string
  email: string
  role: Role
  createdAt: string
}
```

From apps/mobile/contexts/AuthContext.tsx:
```typescript
const TOKEN_KEY = 'auth_token'
const API_BASE = 'http://localhost:3000'

export function useAuth(): AuthContextValue
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add city-area contract and SecureStore service-area hook</name>
  <read_first>
    packages/types/src/index.ts
    apps/mobile/contexts/AuthContext.tsx
  </read_first>
  <files>packages/types/src/index.ts, apps/mobile/contexts/AuthContext.tsx, apps/mobile/hooks/useServiceArea.ts</files>
  <action>
    - In packages/types/src/index.ts, export `CITY_AREAS` as a readonly string array with 5-10 representative names (e.g., Downtown, Uptown, Midtown, Riverside, Old Town, Harbor, Hillcrest, Westside, Eastside, Midtown Heights). Keep it ASCII and stable.
    - In apps/mobile/contexts/AuthContext.tsx, add a `SERVICE_AREA_KEY = 'service_area'` constant next to `TOKEN_KEY` and export it so other modules can import the key.
    - Create apps/mobile/hooks/useServiceArea.ts that:
      - Imports SecureStore, `CITY_AREAS`, and `SERVICE_AREA_KEY`.
      - Exposes `useServiceArea()` returning `{ serviceArea, isLoading, saveServiceArea, clearServiceArea }`.
      - Reads the stored area on mount, sets `isLoading` false after load, and validates that the stored value is in `CITY_AREAS` (if not, clear it).
      - `saveServiceArea(area)` writes to SecureStore and updates state.
      - `clearServiceArea()` deletes the key and sets state to null.
  </action>
  <acceptance_criteria>
    - packages/types/src/index.ts contains `export const CITY_AREAS` with a readonly string array literal.
    - apps/mobile/contexts/AuthContext.tsx contains `export const SERVICE_AREA_KEY = 'service_area'`.
    - apps/mobile/hooks/useServiceArea.ts exports `useServiceArea` and references `SecureStore` and `CITY_AREAS`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck --workspace=packages/types</automated>
  </verify>
  <done>Shared city-area list and SecureStore-backed service-area hook exist and compile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold app routing, onboarding gate, and tabs</name>
  <read_first>
    apps/mobile/app/_layout.tsx
    apps/mobile/app/index.tsx
    .planning/phases/08-mobile-client-job-discovery-acceptance/08-UI-SPEC.md
  </read_first>
  <files>apps/mobile/app/_layout.tsx, apps/mobile/app/index.tsx, apps/mobile/app/(app)/_layout.tsx, apps/mobile/app/(app)/(tabs)/_layout.tsx, apps/mobile/app/(app)/onboarding.tsx</files>
  <action>
    - Update apps/mobile/app/_layout.tsx NavigationGuard to also redirect authenticated users without a stored service area to `/(app)/onboarding`. Use `useServiceArea()` and wait for its loading flag before redirecting.
    - Update apps/mobile/app/index.tsx to redirect to `/(app)/(tabs)/feed` when authenticated. Keep auth guard behavior for unauthenticated users.
    - Create apps/mobile/app/(app)/_layout.tsx that renders a Stack (header hidden) and applies an onboarding gate using `useServiceArea()`; redirect to `/(app)/onboarding` if area is missing.
    - Create apps/mobile/app/(app)/(tabs)/_layout.tsx that defines two Tabs: Feed and Settings. Use Material Community icons `format-list-bulleted` and `cog`, and set tab labels to "Feed" and "Settings".
    - Create apps/mobile/app/(app)/onboarding.tsx per UI-SPEC: heading, helper text, RadioButton.Group for CITY_AREAS, and a contained "Save area" button that persists via `saveServiceArea` and navigates to `/(app)/(tabs)/feed`.
  </action>
  <acceptance_criteria>
    - apps/mobile/app/_layout.tsx references `useServiceArea` and redirects to `/(app)/onboarding` when area is missing.
    - apps/mobile/app/(app)/(tabs)/_layout.tsx contains Tabs for Feed and Settings with the specified icons.
    - apps/mobile/app/(app)/onboarding.tsx renders a RadioButton list from `CITY_AREAS` and a contained "Save area" button.
    - apps/mobile/app/index.tsx redirects to `/(app)/(tabs)/feed` for authenticated users.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck --workspace=apps/mobile</automated>
  </verify>
  <done>Mobile app routes include onboarding gate and bottom tabs with the city/area picker.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace=packages/types`
- `npm run typecheck --workspace=apps/mobile`
</verification>

<success_criteria>
- Service area is stored in SecureStore under `service_area` and required before entering tabs.
- Onboarding screen exists and persists a selected city area from `CITY_AREAS`.
- Tabs for Feed and Settings are reachable after onboarding.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mobile-client-job-discovery-acceptance/08-01-SUMMARY.md`
</output>
