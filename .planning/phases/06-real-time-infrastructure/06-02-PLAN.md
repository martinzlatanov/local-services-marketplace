---
phase: 06-real-time-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/app/api/jobs/[id]/accept/route.ts
  - apps/web/app/api/jobs/[id]/status/route.ts
  - apps/web/components/dashboard/JobDashboard.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, LIFECYCLE-04]
user_setup: []
must_haves:
  truths:
    - "When a provider's acceptance transitions a job to ACCEPTED, the client's browser dashboard reflects the new status without a page refresh and within seconds of the provider action"
    - "When a provider updates a job to IN_PROGRESS or COMPLETED, those transitions are pushed to the client's open dashboard in real time"
    - "Disconnecting and reconnecting a WebSocket client causes the client to refetch current job state (no stale data displayed)"
  artifacts:
    - path: "apps/web/app/api/jobs/[id]/accept/route.ts"
      provides: "Broadcasts JOB_UPDATED event on acceptance"
    - path: "apps/web/app/api/jobs/[id]/status/route.ts"
      provides: "Broadcasts JOB_UPDATED event on status change"
    - path: "apps/web/components/dashboard/JobDashboard.tsx"
      provides: "WebSocket client subscription and state updates"
  key_links:
    - from: "apps/web/app/api/jobs/[id]/accept/route.ts"
      to: "apps/web/lib/ws/server.ts"
      via: "broadcastToUser"
      pattern: "broadcastToUser("
    - from: "apps/web/components/dashboard/JobDashboard.tsx"
      to: "WebSocket Server"
      via: "new WebSocket()"
      pattern: "new WebSocket("
---

<objective>
Wire state transition logic to broadcast events on every job status change; implement client-side subscription in Next.js dashboard (context or hook); verify PENDING→ACCEPTED, ACCEPTED→IN_PROGRESS, IN_PROGRESS→COMPLETED all arrive in real time.

Purpose: Complete the real-time infrastructure by connecting the backend state changes to the frontend dashboard.
Output: Real-time job status updates visible on the client dashboard.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-real-time-infrastructure/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broadcast events on job acceptance</name>
  <files>apps/web/app/api/jobs/[id]/accept/route.ts</files>
  <read_first>
    - apps/web/app/api/jobs/[id]/accept/route.ts
    - apps/web/lib/ws/server.ts
  </read_first>
  <action>
    Update `apps/web/app/api/jobs/[id]/accept/route.ts` to broadcast a `JOB_UPDATED` event to the client when a job is accepted.
    Import `broadcastToUser` from `apps/web/lib/ws/server.ts`.
    After successfully updating the job status in the database, call `broadcastToUser(job.clientId, { type: 'JOB_UPDATED', payload: updatedJob })`.
  </action>
  <verify>
    <automated>grep -c "broadcastToUser" apps/web/app/api/jobs/\[id\]/accept/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/jobs/[id]/accept/route.ts` imports `broadcastToUser`
    - `apps/web/app/api/jobs/[id]/accept/route.ts` calls `broadcastToUser` with the client ID and updated job data
  </acceptance_criteria>
  <done>Job acceptance triggers a WebSocket broadcast to the client.</done>
</task>

<task type="auto">
  <name>Task 2: Broadcast events on job status updates</name>
  <files>apps/web/app/api/jobs/[id]/status/route.ts</files>
  <read_first>
    - apps/web/app/api/jobs/[id]/status/route.ts
    - apps/web/lib/ws/server.ts
  </read_first>
  <action>
    Update `apps/web/app/api/jobs/[id]/status/route.ts` (create if it doesn't exist) to handle provider status updates (IN_PROGRESS, COMPLETED).
    Implement the route handler to update the job status in the database, ensuring the provider owns the job.
    Import `broadcastToUser` from `apps/web/lib/ws/server.ts`.
    After successfully updating the job status, call `broadcastToUser(job.clientId, { type: 'JOB_UPDATED', payload: updatedJob })`.
  </action>
  <verify>
    <automated>grep -c "broadcastToUser" apps/web/app/api/jobs/\[id\]/status/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/jobs/[id]/status/route.ts` exists and handles status updates
    - `apps/web/app/api/jobs/[id]/status/route.ts` imports `broadcastToUser`
    - `apps/web/app/api/jobs/[id]/status/route.ts` calls `broadcastToUser` with the client ID and updated job data
  </acceptance_criteria>
  <done>Job status updates trigger a WebSocket broadcast to the client.</done>
</task>

<task type="auto">
  <name>Task 3: Implement client-side WebSocket subscription</name>
  <files>apps/web/components/dashboard/JobDashboard.tsx</files>
  <read_first>
    - apps/web/components/dashboard/JobDashboard.tsx
  </read_first>
  <action>
    Update `apps/web/components/dashboard/JobDashboard.tsx` (create if it doesn't exist) to connect to the WebSocket server.
    Use a `useEffect` hook to establish the WebSocket connection on mount.
    Handle incoming `JOB_UPDATED` events by updating the local state with the new job data.
    Implement reconnection logic: if the connection drops and reconnects, refetch the current job state from the API to ensure no stale data is displayed.
    Ensure the WebSocket connection is closed on component unmount.
  </action>
  <verify>
    <automated>grep -c "new WebSocket" apps/web/components/dashboard/JobDashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/dashboard/JobDashboard.tsx` establishes a WebSocket connection
    - `apps/web/components/dashboard/JobDashboard.tsx` handles `JOB_UPDATED` events
    - `apps/web/components/dashboard/JobDashboard.tsx` refetches data on reconnection
  </acceptance_criteria>
  <done>Client dashboard receives and processes real-time job status updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted status update requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | Status Update API | mitigate | Ensure the provider requesting the status update owns the job |
</threat_model>

<verification>
- `npm run typecheck` passes
- `grep -c "broadcastToUser" apps/web/app/api/jobs/\[id\]/accept/route.ts` returns > 0
- `grep -c "broadcastToUser" apps/web/app/api/jobs/\[id\]/status/route.ts` returns > 0
- `grep -c "new WebSocket" apps/web/components/dashboard/JobDashboard.tsx` returns > 0
</verification>

<success_criteria>
- Job acceptance and status updates trigger WebSocket broadcasts.
- Client dashboard connects to the WebSocket server and updates state in real time.
- Client dashboard refetches data on reconnection.
</success_criteria>

<output>
After completion, create `.planning/phases/06-real-time-infrastructure/06-02-SUMMARY.md`
</output>
