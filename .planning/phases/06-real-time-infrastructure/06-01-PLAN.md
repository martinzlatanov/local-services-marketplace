---
phase: 06-real-time-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/index.ts
  - apps/web/lib/ws/server.ts
  - apps/web/server.js
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, LIFECYCLE-04]
user_setup: []
must_haves:
  truths:
    - "The backend maintains a WebSocket server"
    - "Clients can connect and authenticate their socket session"
  artifacts:
    - path: "packages/types/src/index.ts"
      provides: "WebSocket event types"
    - path: "apps/web/lib/ws/server.ts"
      provides: "WebSocket server implementation"
    - path: "apps/web/server.js"
      provides: "Custom Next.js server with WebSocket support"
  key_links:
    - from: "apps/web/server.js"
      to: "apps/web/lib/ws/server.ts"
      via: "server upgrade event"
      pattern: "server.on('upgrade'"
---

<objective>
Add WebSocket server to `apps/web` (ws or socket.io); implement connection auth (JWT on handshake); define WebSocket event types in `packages/types`.

Purpose: Enable real-time job status updates for the client dashboard.
Output: A running WebSocket server integrated with Next.js and shared event types.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define WebSocket event types</name>
  <files>packages/types/src/index.ts</files>
  <read_first>
    - packages/types/src/index.ts
  </read_first>
  <action>
    Add WebSocket event types to `packages/types/src/index.ts`.
    Define `WsEvent` type with `type` and `payload` properties.
    Define specific event types: `JOB_UPDATED` with payload containing the updated job data.
    Export the types.
  </action>
  <verify>
    <automated>grep -c "export type WsEvent" packages/types/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/types/src/index.ts` contains `export type WsEvent`
    - `packages/types/src/index.ts` contains `JOB_UPDATED`
  </acceptance_criteria>
  <done>WebSocket event types are defined and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Implement WebSocket server</name>
  <files>apps/web/lib/ws/server.ts</files>
  <read_first>
    - apps/web/lib/auth.ts
  </read_first>
  <action>
    Create `apps/web/lib/ws/server.ts`.
    Implement a WebSocket server using the `ws` library.
    Implement connection authentication using JWT from the handshake request (cookie or header).
    Use `verifyJwt` from `apps/web/lib/auth.ts` to authenticate the connection.
    Store authenticated connections with their user ID for targeted broadcasting.
    Export a function to initialize the WebSocket server and a function to broadcast events to specific users.
  </action>
  <verify>
    <automated>grep -c "export function initWsServer" apps/web/lib/ws/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/ws/server.ts` contains `export function initWsServer`
    - `apps/web/lib/ws/server.ts` contains `export function broadcastToUser`
    - `apps/web/lib/ws/server.ts` imports `verifyJwt`
  </acceptance_criteria>
  <done>WebSocket server implementation is complete with authentication and broadcasting capabilities.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate WebSocket server with Next.js</name>
  <files>apps/web/server.js, apps/web/package.json</files>
  <read_first>
    - apps/web/package.json
  </read_first>
  <action>
    Create a custom Next.js server in `apps/web/server.js` to handle WebSocket upgrades.
    Import `initWsServer` from `apps/web/lib/ws/server.ts`.
    Listen for the `upgrade` event on the HTTP server and pass it to the WebSocket server.
    Update `apps/web/package.json` scripts to use `node server.js` instead of `next dev` and `next start`.
    Install `ws` and `@types/ws` as dependencies in `apps/web`.
  </action>
  <verify>
    <automated>grep -c "server.on('upgrade'" apps/web/server.js</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/server.js` contains `server.on('upgrade'`
    - `apps/web/package.json` scripts use `node server.js`
    - `apps/web/package.json` dependencies include `ws`
  </acceptance_criteria>
  <done>Next.js is configured to run with a custom server that supports WebSockets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> WebSocket Server | Untrusted WebSocket connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | WebSocket Server | mitigate | Authenticate connections using JWT on handshake |
| T-06-02 | Information Disclosure | WebSocket Server | mitigate | Only broadcast events to the specific user ID associated with the connection |
</threat_model>

<verification>
- `npm run typecheck` passes
- `grep -c "export type WsEvent" packages/types/src/index.ts` returns > 0
- `grep -c "export function initWsServer" apps/web/lib/ws/server.ts` returns > 0
- `grep -c "server.on('upgrade'" apps/web/server.js` returns > 0
</verification>

<success_criteria>
- WebSocket event types are defined in `packages/types`.
- WebSocket server is implemented with JWT authentication.
- Next.js is configured to run with a custom server supporting WebSockets.
</success_criteria>

<output>
After completion, create `.planning/phases/06-real-time-infrastructure/06-01-SUMMARY.md`
</output>
