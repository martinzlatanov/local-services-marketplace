---
phase: 12
plan: "12-03"
title: "Auth pages — login + register"
wave: 2
depends_on:
  - "12-01"
files_modified:
  - apps/web/app/login/page.tsx
  - apps/web/app/register/page.tsx
autonomous: true
requirements:
  - UI-03
---

<objective>
Rebuild both auth pages to the approved sketch designs:
- Login: `surface-900` left panel (logo-dot, "Welcome back.", icon-tile feature rows, stat row) + `surface-50` right form with `surface-900` submit button
- Register: same left panel shell with "Join the community." copy + right panel with 2-column role picker cards (surface-900 active border + filled radio dot) and password strength bar

All form logic, validation state, error handling, and routing are preserved exactly.
</objective>

<tasks>

<task id="12-03-T1" type="execute">
  <title>Rebuild login/page.tsx — surface-900 split panel</title>

  <read_first>
    - apps/web/app/login/page.tsx (all existing state, handlers, error logic to preserve)
    - .planning/sketches/03-auth-login.html (source of truth for visual structure)
    - apps/web/app/globals.css (confirm eyebrow, nav-sticky classes exist)
  </read_first>

  <action>
  Rewrite the JSX return in `apps/web/app/login/page.tsx`. Preserve ALL existing:
  - imports (Link, useRouter, useState, useAuth, Wrench, Loader2, XCircle, ApiErrorResponse)
  - state variables (email, password, fieldErrors, topError, submitting)
  - `handleSubmit` function (unchanged)
  - loading spinner early-return
  - error variables (emailError, passwordError)

  Replace only the `return (...)` JSX. New structure:

  **Loading state** (unchanged):
  ```tsx
  if (isLoading) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-surface-50">
        <div className="flex flex-col items-center gap-3">
          <Loader2 className="h-8 w-8 text-brand-600 animate-spin" aria-label="Loading" />
          <p className="text-surface-600">Loading...</p>
        </div>
      </div>
    )
  }
  ```

  **Main return** — remove `Wrench` from the import if it's only used in the old left panel (replace with logo-dot span). Keep Loader2 and XCircle.

  ```tsx
  return (
    <div className="min-h-screen flex items-stretch">
      {/* LEFT PANEL */}
      <div className="hidden lg:flex lg:w-1/2 bg-surface-900 text-white flex-col justify-between px-16 py-20">
        <div className="flex items-center gap-1.5 font-extrabold text-lg tracking-[-0.4px]">
          <span className="w-2.5 h-2.5 rounded-full bg-brand-500 inline-block" />
          LocalPro
        </div>
        <div>
          <p className="eyebrow eyebrow-brand mb-4 text-surface-500">Sign in</p>
          <h2 className="text-[clamp(32px,3.5vw,48px)] font-extrabold tracking-[-1.5px] leading-[1.1] mb-4">
            Welcome back.
          </h2>
          <p className="text-[16px] text-surface-400 leading-relaxed max-w-[360px] mb-10">
            Access your jobs, track progress, and connect with local professionals.
          </p>
          <div className="flex flex-col gap-4">
            {[
              { icon: '🎯', title: 'Post & track jobs', desc: 'Manage the full lifecycle from one dashboard.' },
              { icon: '⚡', title: 'Real-time updates', desc: 'Live status changes via WebSocket.' },
              { icon: '✨', title: 'Trusted providers', desc: 'Verified local professionals in 10 areas.' },
            ].map((f) => (
              <div key={f.icon} className="flex items-start gap-3.5">
                <div className="w-8 h-8 rounded-lg bg-surface-800 border border-surface-700 flex items-center justify-center flex-shrink-0 text-sm">
                  {f.icon}
                </div>
                <div>
                  <p className="text-[14px] font-semibold text-white mb-0.5">{f.title}</p>
                  <p className="text-[13px] text-surface-400">{f.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
        <div className="flex gap-8 pt-8 border-t border-surface-800">
          {[{ num: '10', label: 'City areas' }, { num: '8', label: 'Categories' }, { num: '<1h', label: 'Response' }].map((s) => (
            <div key={s.label}>
              <p className="text-[24px] font-extrabold tracking-[-1px] font-mono"><span className="text-brand-500">{s.num}</span></p>
              <p className="text-[12px] text-surface-500 mt-0.5">{s.label}</p>
            </div>
          ))}
        </div>
      </div>

      {/* RIGHT PANEL */}
      <div className="w-full lg:w-1/2 flex items-center justify-center py-16 px-8 bg-surface-50">
        <div className="w-full max-w-[440px]">
          <p className="eyebrow mb-5">Step 1 of 1</p>
          <h1 className="text-[30px] font-extrabold tracking-[-0.8px] text-surface-900 mb-1.5">Sign in</h1>
          <p className="text-[14px] text-surface-500 mb-7">Enter your credentials to continue.</p>

          {topError && (
            <div role="alert" className="mb-6 flex gap-3 bg-red-50 border border-red-200 text-red-800 rounded-[var(--radius-btn)] p-4">
              <XCircle className="h-5 w-5 flex-shrink-0 mt-0.5" aria-hidden="true" />
              <span>{topError}</span>
            </div>
          )}

          <form onSubmit={handleSubmit} className="space-y-4">
            <div>
              <label className="block text-[12px] font-bold tracking-[0.04em] uppercase text-surface-600 mb-1.5" htmlFor="email">
                Email
              </label>
              <input
                id="email"
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                autoComplete="email"
                placeholder="you@example.com"
                aria-invalid={!!emailError}
                aria-describedby={emailError ? 'email-error' : undefined}
                className="w-full bg-surface-0 border border-surface-200 text-surface-900 rounded-[var(--radius-input)] px-3.5 py-2.5 text-[14px] focus:outline-none focus:border-brand-500 focus:ring-[3px] focus:ring-brand-500/10 transition-all"
              />
              {emailError && <span id="email-error" className="text-red-600 text-sm mt-1 block">{emailError}</span>}
            </div>
            <div>
              <label className="block text-[12px] font-bold tracking-[0.04em] uppercase text-surface-600 mb-1.5" htmlFor="password">
                Password
              </label>
              <input
                id="password"
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                autoComplete="current-password"
                placeholder="Your password"
                aria-invalid={!!passwordError}
                aria-describedby={passwordError ? 'password-error' : undefined}
                className="w-full bg-surface-0 border border-surface-200 text-surface-900 rounded-[var(--radius-input)] px-3.5 py-2.5 text-[14px] focus:outline-none focus:border-brand-500 focus:ring-[3px] focus:ring-brand-500/10 transition-all"
              />
              {passwordError && <span id="password-error" className="text-red-600 text-sm mt-1 block">{passwordError}</span>}
            </div>
            <button
              type="submit"
              disabled={submitting}
              className="w-full bg-surface-900 text-white py-3 px-4 rounded-[var(--radius-btn)] hover:opacity-[0.88] disabled:opacity-50 font-bold text-[15px] tracking-[-0.2px] transition-opacity flex items-center justify-center gap-2 mt-2"
            >
              {submitting ? (
                <>
                  <Loader2 className="h-4 w-4 animate-spin" aria-hidden="true" />
                  Signing in…
                </>
              ) : (
                'Sign in →'
              )}
            </button>
          </form>

          <p className="mt-5 text-center text-[14px] text-surface-500">
            Don&apos;t have an account?{' '}
            <Link href="/register" className="text-surface-900 font-bold hover:underline">
              Create one →
            </Link>
          </p>
        </div>
      </div>
    </div>
  )
  ```

  Remove the `Wrench` import if it's no longer used after this change. Keep all other imports.
  </action>

  <acceptance_criteria>
    - `apps/web/app/login/page.tsx` does NOT contain `bg-gradient-to-br`
    - `apps/web/app/login/page.tsx` does NOT contain `from-brand-700`
    - `apps/web/app/login/page.tsx` does NOT contain `bg-brand-600`
    - `apps/web/app/login/page.tsx` contains `bg-surface-900` (left panel background)
    - `apps/web/app/login/page.tsx` contains `bg-surface-900` on the submit button
    - `apps/web/app/login/page.tsx` contains `focus:ring-brand-500/10` (teal focus ring)
    - `apps/web/app/login/page.tsx` contains `Welcome back.`
    - `apps/web/app/login/page.tsx` contains `handleSubmit` (logic preserved)
    - `apps/web/app/login/page.tsx` contains `router.push('/dashboard')` (routing preserved)
  </acceptance_criteria>
</task>

<task id="12-03-T2" type="execute">
  <title>Rebuild register/page.tsx — role picker cards + password strength bar</title>

  <read_first>
    - apps/web/app/register/page.tsx (all existing state, handlers, error logic to preserve)
    - .planning/sketches/06-auth-register.html (source of truth — role picker cards, strength bar)
  </read_first>

  <action>
  Rewrite the JSX return in `apps/web/app/register/page.tsx`. Preserve ALL existing:
  - imports (Link, useRouter, useState, useAuth, Role, ApiErrorResponse, User, Wrench, Loader2, XCircle)
  - state variables (email, password, role, fieldErrors, topError, submitting)
  - `handleSubmit` function (unchanged)
  - error variables (emailError, passwordError, roleError)

  Add one new state variable for password strength display only:
  ```tsx
  const strengthPercent = Math.min(100, password.length * 8 + ((/[^a-zA-Z0-9]/.test(password)) ? 20 : 0))
  const strengthLabel = strengthPercent < 40 ? 'Weak' : strengthPercent < 70 ? 'Fair' : strengthPercent < 90 ? 'Good' : 'Strong'
  const strengthColor = strengthPercent < 40 ? '#ef4444' : strengthPercent < 70 ? '#f59e0b' : strengthPercent < 90 ? '#14b8a6' : '#22c55e'
  ```
  (These are derived values from existing `password` state — no new useState needed.)

  **Left panel** — identical shell to login but with "Join the community." heading copy and "Create account" eyebrow tag.

  **Right panel role picker** — replace the current `<fieldset>` role selector with a 2-column card grid:
  ```tsx
  <p className="text-[12px] font-bold tracking-[0.06em] uppercase text-surface-600 mb-2.5">I want to…</p>
  <div className="grid grid-cols-2 gap-2 mb-6">
    {([
      { value: Role.CLIENT,   icon: '🏠', title: 'Post jobs',  desc: 'I need local help with tasks around my home or business.' },
      { value: Role.PROVIDER, icon: '🔧', title: 'Find work',  desc: 'I offer a skill and want to find jobs in my area.' },
    ] as const).map((opt) => (
      <label
        key={opt.value}
        className={`relative p-4 border rounded-[10px] cursor-pointer flex flex-col gap-1.5 transition-all ${
          role === opt.value
            ? 'border-surface-900 shadow-[0_0_0_1px_#0f172a] bg-surface-0'
            : 'border-surface-200 bg-surface-0 hover:border-surface-300'
        }`}
      >
        <input type="radio" name="role" value={opt.value} checked={role === opt.value} onChange={(e) => setRole(e.target.value as Role)} className="sr-only" />
        {/* Radio check dot */}
        <span className={`absolute top-2.5 right-2.5 w-[18px] h-[18px] rounded-full border flex items-center justify-center ${
          role === opt.value ? 'bg-surface-900 border-surface-900' : 'border-surface-200 bg-surface-0'
        }`}>
          {role === opt.value && <span className="w-1.5 h-1.5 rounded-full bg-white inline-block" />}
        </span>
        <span className="text-xl mb-0.5">{opt.icon}</span>
        <span className={`text-[14px] font-bold ${role === opt.value ? 'text-surface-900' : 'text-surface-800'}`}>{opt.title}</span>
        <span className="text-[12px] text-surface-400 leading-snug">{opt.desc}</span>
      </label>
    ))}
  </div>
  {roleError && <span className="text-red-600 text-sm mb-3 block">{roleError}</span>}
  ```

  **Password field** — after the password `<input>`, add strength bar:
  ```tsx
  <div className="mt-1.5 h-[3px] rounded-sm bg-surface-100 overflow-hidden">
    <div style={{ width: `${strengthPercent}%`, background: strengthColor }} className="h-full rounded-sm transition-all duration-300" />
  </div>
  <p className="text-[11px] text-surface-400 mt-1">{strengthLabel}{strengthPercent >= 70 ? ' — add a symbol to make it stronger' : ''}</p>
  ```

  **Submit button**: change `bg-brand-600 hover:bg-brand-700 disabled:bg-brand-300` → `bg-surface-900 hover:opacity-[0.88] disabled:opacity-50 transition-opacity`

  **Sign-in link**: change `text-brand-600 hover:text-brand-700` → `text-surface-900 font-bold hover:underline`

  Remove `User` import if only used in the old role selector radio cards (replaced by emoji). Keep Wrench if still used, otherwise remove.
  </action>

  <acceptance_criteria>
    - `apps/web/app/register/page.tsx` does NOT contain `bg-gradient-to-br`
    - `apps/web/app/register/page.tsx` does NOT contain `from-brand-700`
    - `apps/web/app/register/page.tsx` does NOT contain `bg-brand-600`
    - `apps/web/app/register/page.tsx` does NOT contain `border-brand-500`
    - `apps/web/app/register/page.tsx` does NOT contain `ring-brand-500` (old ring active state on role card)
    - `apps/web/app/register/page.tsx` contains `border-surface-900` (active role card border)
    - `apps/web/app/register/page.tsx` contains `bg-surface-900` (submit button + active radio dot)
    - `apps/web/app/register/page.tsx` contains `strengthPercent`
    - `apps/web/app/register/page.tsx` contains `Join the community`
    - `apps/web/app/register/page.tsx` contains `handleSubmit` (logic preserved)
    - `apps/web/app/register/page.tsx` contains `router.push('/dashboard')` (routing preserved)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep "bg-gradient-to-br\|from-brand-700\|bg-brand-600\|bg-accent-500" apps/web/app/login/page.tsx` → no output
2. `grep "bg-gradient-to-br\|from-brand-700\|bg-brand-600\|border-brand-500\|ring-brand-500" apps/web/app/register/page.tsx` → no output
3. `grep "bg-surface-900" apps/web/app/login/page.tsx` → at least 2 matches (left panel + button)
4. `grep "strengthPercent" apps/web/app/register/page.tsx` → at least 1 match
5. `npx tsc --noEmit` passes
</verification>

<must_haves>
  <truths>
    - All form submission logic, validation, and error display is preserved unchanged
    - Both pages use `bg-surface-900` for primary CTA (submit button)
    - No teal gradient on left panels
    - Register role picker uses `border-surface-900` active state (not `border-brand-500`)
    - Password strength bar is purely presentational (derived from existing `password` state)
  </truths>
</must_haves>
