---
phase: 12
plan: "12-04"
title: "Client dashboard"
wave: 2
depends_on:
  - "12-01"
files_modified:
  - apps/web/components/dashboard/ClientDashboard.tsx
autonomous: true
requirements:
  - UI-04
---

<objective>
Rebuild `ClientDashboard.tsx` layout to the approved sketch: eyebrow/title page header, 4-cell bordered stats strip, left numbered panel ("01 /") with the post-job form, right panel with jobs data table tab bar (All / Active / Completed / Reviews) and search input. All data fetching, state, filtering logic, and child component calls are preserved.
</objective>

<tasks>

<task id="12-04-T1" type="execute">
  <title>Rebuild ClientDashboard layout — stats strip + numbered panels</title>

  <read_first>
    - apps/web/components/dashboard/ClientDashboard.tsx (all existing state, tabs, filter logic — preserve everything)
    - .planning/sketches/04-client-dashboard.html (source of truth — stats strip, numbered panels, data table tab bar)
  </read_first>

  <action>
  Rewrite only the `return (...)` JSX in `ClientDashboard.tsx`. Preserve ALL:
  - imports (useState, useEffect, useCallback, JobDto, JobStatus, Role, ReviewDTO, JobPostingForm, JobDashboard, ReviewDisplay)
  - state (activeTab, activeJobTab, receivedReviews, averageRatings, reviewsLoading)
  - fetchReceivedReviews, useEffect, filteredJobs, counts

  New return structure:

  ```tsx
  return (
    <div>
      {/* Page header */}
      <div className="mb-8">
        <p className="eyebrow mb-3">Client Dashboard</p>
        <h1 className="text-[32px] font-extrabold tracking-[-1px] text-surface-900">My Jobs</h1>
        <p className="text-[14px] text-surface-500 mt-1">Post new jobs and track their progress in real time.</p>
      </div>

      {/* Stats strip */}
      <div className="border border-surface-200 rounded-[var(--radius-card)] overflow-hidden mb-8">
        <div className="grid grid-cols-4">
          {([
            { label: 'Posted',    value: jobs.length },
            { label: 'Active',    value: counts.Active },
            { label: 'Completed', value: counts.Completed },
            { label: 'Pending',   value: jobs.filter((j) => j.status === JobStatus.PENDING).length },
          ] as const).map((stat, i) => (
            <div key={stat.label} className={`px-6 py-5 ${i < 3 ? 'border-r border-surface-200' : ''}`}>
              <p className="text-[28px] font-extrabold tracking-[-1px] text-surface-900 font-mono">{stat.value}</p>
              <p className="text-[12px] text-surface-500 mt-1">{stat.label}</p>
            </div>
          ))}
        </div>
      </div>

      {/* Two-panel grid */}
      <div className="grid lg:grid-cols-[2fr_3fr] gap-0 border border-surface-200 rounded-[var(--radius-card)] overflow-hidden">

        {/* LEFT: Post-job form panel */}
        <div className="border-r border-surface-200 p-6">
          <p className="text-[11px] font-bold tracking-[0.06em] text-surface-400 mb-4">01 /</p>
          <h2 className="text-[16px] font-bold tracking-[-0.3px] text-surface-900 mb-5">Post a Job</h2>
          <JobPostingForm onSuccess={onJobPosted} />
        </div>

        {/* RIGHT: Jobs table panel */}
        <div className="p-6">
          <p className="text-[11px] font-bold tracking-[0.06em] text-surface-400 mb-4">02 /</p>
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-[16px] font-bold tracking-[-0.3px] text-surface-900">My Jobs</h2>
          </div>

          {/* Tab bar */}
          <div className="flex border-b border-surface-200 mb-4 gap-0">
            {(['jobs', 'reviews'] as const).map((tab) => (
              <button
                key={tab}
                onClick={() => setActiveTab(tab)}
                className={`px-4 py-2.5 text-[13px] font-medium border-b-2 -mb-px transition-colors ${
                  activeTab === tab
                    ? 'border-surface-900 text-surface-900'
                    : 'border-transparent text-surface-500 hover:text-surface-700'
                }`}
              >
                {tab === 'jobs' ? 'Jobs' : 'Reviews'}
                {tab === 'reviews' && receivedReviews.length > 0 && (
                  <span className="ml-1.5 text-[11px] bg-surface-100 text-surface-600 px-1.5 py-0.5 rounded-full">
                    {receivedReviews.length}
                  </span>
                )}
              </button>
            ))}
          </div>

          {activeTab === 'jobs' && (
            <>
              {/* Job sub-tabs */}
              <div className="flex gap-1 mb-4">
                {JOB_TABS.map((tab) => (
                  <button
                    key={tab}
                    onClick={() => setActiveJobTab(tab)}
                    className={`px-3 py-1.5 text-[12px] font-medium rounded-md transition-colors ${
                      activeJobTab === tab
                        ? 'bg-surface-900 text-white'
                        : 'bg-surface-100 text-surface-600 hover:bg-surface-200'
                    }`}
                  >
                    {tab} <span className="opacity-60">({counts[tab]})</span>
                  </button>
                ))}
              </div>

              {jobs.length === 0 && (
                <p className="text-sm text-surface-500 bg-surface-50 border border-surface-200 rounded-[var(--radius-btn)] px-4 py-3 mb-4">
                  No jobs yet — post one using the form on the left.
                </p>
              )}

              <JobDashboard jobs={filteredJobs} userRole={Role.CLIENT} onJobUpdate={onJobUpdate} />
            </>
          )}

          {activeTab === 'reviews' && (
            <ReviewDisplay
              userId={userId}
              reviews={receivedReviews}
              averageRatings={averageRatings}
              isLoading={reviewsLoading}
              reviewType="provider"
            />
          )}
        </div>
      </div>
    </div>
  )
  ```
  </action>

  <acceptance_criteria>
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `01 /`
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `02 /`
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `eyebrow`
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `Stats strip` OR `grid-cols-4` (stats grid)
    - `apps/web/components/dashboard/ClientDashboard.tsx` does NOT contain `bg-surface-100 p-1 rounded` (old pill tab container)
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `border-surface-900` (active tab underline)
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `fetchReceivedReviews` (logic preserved)
    - `apps/web/components/dashboard/ClientDashboard.tsx` contains `filteredJobs` (filter logic preserved)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep "01 /" apps/web/components/dashboard/ClientDashboard.tsx` → match
2. `grep "bg-surface-100 p-1" apps/web/components/dashboard/ClientDashboard.tsx` → no output (old pill tabs gone)
3. `grep "border-surface-900" apps/web/components/dashboard/ClientDashboard.tsx` → at least 1
4. `grep "fetchReceivedReviews" apps/web/components/dashboard/ClientDashboard.tsx` → at least 1
5. `npx tsc --noEmit` passes
</verification>

<must_haves>
  <truths>
    - All review fetching logic is preserved (fetchReceivedReviews, useEffect, ReviewDisplay usage)
    - All job filtering logic is preserved (filteredJobs, counts, activeJobTab)
    - JobPostingForm and JobDashboard child components are still rendered with the same props
    - No amber class used anywhere in this file
  </truths>
</must_haves>
