---
phase: 10-end-to-end-polish-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - E2E-TEST-CHECKLIST.md
  - apps/mobile/app.json
autonomous: true
requirements:
  - DEPLOY-03
must_haves:
  truths:
    - "Full lifecycle test can be performed manually in production"
    - "Test covers: register client → post job → register provider → accept → start → finish"
    - "Real-time updates verified in production environment"
  artifacts:
    - path: "E2E-TEST-CHECKLIST.md"
      provides: "End-to-end test checklist"
      min_lines: 50
    - path: "apps/mobile/app.json"
      provides: "Mobile app configuration"
      contains: "expo"
  key_links:
    - from: "Web client"
      to: "Production Vercel URL"
      via: "Register and post job"
      pattern: "register.*post.*job"
    - from: "Mobile provider"
      to: "Production API"
      via: "Accept, start, finish job"
      pattern: "accept.*start.*finish"
---

<objective>
Create end-to-end test checklist and verify the mobile app can connect to production API.

Purpose: Document a complete manual testing checklist that validates the full job lifecycle in production. Also ensure the mobile app is configured to point to the production API URL.

Output: E2E test checklist, mobile app production config
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-end-to-end-polish-deployment/10-CONTEXT.md
@.planning/phases/09-mobile-client-active-job-execution/09-03-SUMMARY.md

## Full Lifecycle Test (from ROADMAP)
1. Register as client on web (production URL)
2. Post a job with category, description, timeframe, city/area
3. Register as provider on mobile
4. Accept the job from mobile feed
5. Tap "Start Work" to transition to IN_PROGRESS
6. Tap "Finish Work" to transition to COMPLETED
7. Verify real-time updates on web dashboard

## Mobile App Configuration
**app.json:** Expo configuration
**API base URL:** Currently likely pointing to localhost — needs production URL
**Environment:** Need to configure `API_BASE` or use `expo-constants` for environment-specific URLs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test checklist</name>
  <files>E2E-TEST-CHECKLIST.md</files>
  <action>
Create `E2E-TEST-CHECKLIST.md` with a comprehensive manual testing checklist for the full job lifecycle.

**Implementation:**
```markdown
# End-to-End Test Checklist — Production

## Prerequisites
- [ ] Web app deployed to Vercel (https://your-app.vercel.app)
- [ ] Neon production database provisioned and migrated
- [ ] Mobile app configured with production API URL
- [ ] Two user accounts ready (one client, one provider)

## Test Scenario: Full Job Lifecycle

### Phase 1: Client Registers and Posts Job
- [ ] Open production web app in browser
- [ ] Click "Register" and create client account
- [ ] Verify redirect to dashboard after registration
- [ ] Click "Post a Job"
- [ ] Fill form: Category (e.g., "Plumbing"), Description, Timeframe, City/Area
- [ ] Submit job
- [ ] Verify job appears on dashboard with status "PENDING"
- [ ] Note the job ID for later verification

### Phase 2: Provider Registers and Accepts Job
- [ ] Open mobile app (Expo dev or production build)
- [ ] Register as provider (select service area matching job's city/area)
- [ ] Verify "Feed" tab shows the posted job
- [ ] Tap job to view details
- [ ] Tap "Accept job"
- [ ] Verify job disappears from Feed
- [ ] Navigate to "Active Jobs" tab
- [ ] Verify job appears with status "ACCEPTED"

### Phase 3: Provider Starts Work
- [ ] In "Active Jobs" tab, tap the accepted job
- [ ] Verify detail screen shows "Start Work" button
- [ ] Tap "Start Work"
- [ ] Verify button changes to "Finish Work"
- [ ] Verify job status now shows "IN_PROGRESS"

### Phase 4: Provider Completes Work
- [ ] Tap "Finish Work"
- [ ] Verify job disappears from "Active Jobs" tab (or status changes to COMPLETED)
- [ ] Verify no errors or crashes

### Phase 5: Verify Real-Time Updates
- [ ] Open web dashboard in browser (client account)
- [ ] Keep dashboard open during Phase 2-4
- [ ] Verify job status updates in real-time:
  - [ ] Status changes from PENDING → ACCEPTED (when provider accepts)
  - [ ] Status changes from ACCEPTED → IN_PROGRESS (when provider starts)
  - [ ] Status changes from IN_PROGRESS → COMPLETED (when provider finishes)
- [ ] Verify no page refresh needed for updates

## Success Criteria
- [ ] All steps completed without errors
- [ ] Real-time updates working on web dashboard
- [ ] Job lifecycle fully functional in production
```

**Why:** Provides a structured checklist for manual E2E testing. Can be run after deployment to verify the system works end-to-end.
  </action>
  <verify>
    <automated>test -f E2E-TEST-CHECKLIST.md && grep -c "Phase\|Verify" E2E-TEST-CHECKLIST.md</automated>
  </verify>
  <done>E2E-TEST-CHECKLIST.md created with 50+ lines, covers full lifecycle and real-time verification</done>
</task>

<task type="auto">
  <name>Task 2: Configure mobile app for production API</name>
  <files>apps/mobile/app.json, apps/mobile/contexts/AuthContext.tsx</files>
  <action>
Check and update the mobile app to use production API URL.

**Check current API base URL:**
```bash
grep -n "API_BASE" apps/mobile/contexts/AuthContext.tsx
```

**If using `expo-constants` for environment config:**
1. Install: `npm install expo-constants`
2. Update `app.json` to include `expo-constants` plugin
3. Create `app.config.js` with environment-specific API URL:

```javascript
export default {
  name: 'local-services',
  version: '1.0.0',
  extra: {
    apiUrl: process.env.API_URL || 'https://your-app.vercel.app',
  },
}
```

**If using simple constant:**
Update `apps/mobile/contexts/AuthContext.tsx`:
```typescript
// Before: const API_BASE = 'http://localhost:3000'
// After:
const API_BASE = process.env.EXPO_PUBLIC_API_URL || 'https://your-app.vercel.app'
```

**Simpler approach (recommended for now):**
Just document the production URL in a comment and let the user configure it:
```typescript
// Production: https://your-app.vercel.app
// Development: http://localhost:3000
const API_BASE = 'https://your-app.vercel.app' // Change to localhost:3000 for dev
```

**Why:** The mobile app needs to point to the production API URL after deployment. This task ensures it's configured correctly.
  </action>
  <verify>
    <automated>grep -c "API_BASE\|apiUrl" apps/mobile/contexts/AuthContext.tsx apps/mobile/app.json 2>/dev/null || echo "0"</automated>
  </verify>
  <done>Mobile app configured with production API URL, or documented for manual configuration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile App → Production API | API requests over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | API URL config | mitigate | Use HTTPS for production, validate URL in config |
| T-10-06 | Information Disclosure | API credentials | mitigate | Use SecureStore for tokens, never log tokens in production |
</threat_model>

<verification>
- E2E checklist can be followed end-to-end without missing steps
- Mobile app connects to production API successfully
</verification>

<success_criteria>
- E2E-TEST-CHECKLIST.md created with complete testing checklist
- Mobile app configured for production API (or documented)
- Ready for production E2E testing
</success_criteria>

<output>
After completion, create `.planning/phases/10-end-to-end-polish-deployment/10-03-SUMMARY.md`
</output>
