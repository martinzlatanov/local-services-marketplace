---
phase: 07-web-client-job-posting-dashboard
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/components/dashboard/JobDashboard.tsx
  - apps/web/components/dashboard/JobCard.tsx
autonomous: true
requirements: [JOB-POST-04]
user_setup: []
must_haves:
  truths:
    - "A logged-in client sees all their jobs with live status on a dashboard"
  artifacts:
    - path: "apps/web/components/dashboard/JobDashboard.tsx"
      provides: "Dashboard component displaying the list of jobs"
    - path: "apps/web/components/dashboard/JobCard.tsx"
      provides: "Card component displaying individual job details"
  key_links:
    - from: "apps/web/components/dashboard/JobDashboard.tsx"
      to: "apps/web/components/dashboard/JobCard.tsx"
      via: "renders"
      pattern: "<JobCard"
---

<objective>
Implement the dashboard layout and job card components to display the user's posted jobs with real-time status updates.

Purpose: Provide a clear and responsive view of the client's jobs.
Output: A dashboard displaying job cards with visual status indicators.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-client-job-posting-dashboard/07-CONTEXT.md
@.planning/phases/07-web-client-job-posting-dashboard/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Job Card component</name>
  <files>apps/web/components/dashboard/JobCard.tsx</files>
  <read_first>
    - packages/types/src/index.ts
  </read_first>
  <action>
    Create `apps/web/components/dashboard/JobCard.tsx`.
    Implement a card component that takes a `JobDto` as a prop.
    Display the job category, description, timeframe, city/area, and current status.
    Use clear visual indicators (e.g., badges with different colors) for the status (`PENDING`, `ACCEPTED`, `IN_PROGRESS`, `COMPLETED`).
  </action>
  <verify>
    <automated>grep -c "JobDto" apps/web/components/dashboard/JobCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/dashboard/JobCard.tsx` takes a `JobDto` prop
    - `apps/web/components/dashboard/JobCard.tsx` displays job details and status
    - `apps/web/components/dashboard/JobCard.tsx` uses visual indicators for status
  </acceptance_criteria>
  <done>Job card component is created and displays job details.</done>
</task>

<task type="auto">
  <name>Task 2: Update Job Dashboard component</name>
  <files>apps/web/components/dashboard/JobDashboard.tsx</files>
  <read_first>
    - apps/web/components/dashboard/JobDashboard.tsx
    - apps/web/components/dashboard/JobCard.tsx
  </read_first>
  <action>
    Update `apps/web/components/dashboard/JobDashboard.tsx` to render a list or grid of `JobCard` components.
    Ensure the component handles empty states (e.g., "No jobs posted yet").
    Verify that the real-time WebSocket updates (implemented in Phase 6) correctly update the job data passed to the `JobCard` components.
  </action>
  <verify>
    <automated>grep -c "<JobCard" apps/web/components/dashboard/JobDashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/dashboard/JobDashboard.tsx` renders `JobCard` components
    - `apps/web/components/dashboard/JobDashboard.tsx` handles empty states
    - `apps/web/components/dashboard/JobDashboard.tsx` updates job data on WebSocket events
  </acceptance_criteria>
  <done>Dashboard component displays the list of jobs using the job card component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | Rendering untrusted job data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Job Dashboard | mitigate | Ensure the API only returns jobs belonging to the authenticated client (handled by API) |
| T-07-04 | Cross-Site Scripting (XSS) | Job Card | mitigate | React automatically escapes rendered text content |
</threat_model>

<verification>
- `npm run typecheck` passes
- `grep -c "JobDto" apps/web/components/dashboard/JobCard.tsx` returns > 0
- `grep -c "<JobCard" apps/web/components/dashboard/JobDashboard.tsx` returns > 0
</verification>

<success_criteria>
- Client sees all their jobs on the dashboard.
- Job cards display clear visual status indicators.
- Real-time status updates are reflected on the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-client-job-posting-dashboard/07-02-SUMMARY.md`
</output>
