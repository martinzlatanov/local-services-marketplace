---
phase: 07-web-client-job-posting-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/dashboard/page.tsx
  - apps/web/components/dashboard/JobPostingForm.tsx
autonomous: true
requirements: [JOB-POST-04]
user_setup: []
must_haves:
  truths:
    - "A logged-in client fills out and submits the job posting form (category from dropdown, description, timeframe, city/area) and sees the new job appear on their dashboard with status PENDING"
  artifacts:
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard page layout and data fetching"
    - path: "apps/web/components/dashboard/JobPostingForm.tsx"
      provides: "Form for posting new jobs"
  key_links:
    - from: "apps/web/components/dashboard/JobPostingForm.tsx"
      to: "apps/web/app/api/jobs/route.ts"
      via: "fetch POST"
      pattern: "fetch.*api/jobs"
---

<objective>
Implement the job posting form and integrate it into the client dashboard.

Purpose: Allow authenticated clients to post new jobs.
Output: A functional job posting form on the dashboard that creates jobs via the API.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-client-job-posting-dashboard/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Job Posting Form component</name>
  <files>apps/web/components/dashboard/JobPostingForm.tsx</files>
  <read_first>
    - packages/types/src/index.ts
    - apps/web/lib/db/categories.ts
  </read_first>
  <action>
    Create `apps/web/components/dashboard/JobPostingForm.tsx`.
    Implement a form with fields for category (dropdown using `jobCategoryEnum` values), description (textarea), timeframe (text input), and city/area (text input).
    On submit, send a POST request to `/api/jobs` with the form data.
    Handle loading states and display success/error messages.
    On success, clear the form and trigger a callback to refresh the job list.
  </action>
  <verify>
    <automated>grep -c "fetch.*api/jobs" apps/web/components/dashboard/JobPostingForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/dashboard/JobPostingForm.tsx` contains a form with category, description, timeframe, and cityArea fields
    - `apps/web/components/dashboard/JobPostingForm.tsx` sends a POST request to `/api/jobs`
    - `apps/web/components/dashboard/JobPostingForm.tsx` handles success and error states
  </acceptance_criteria>
  <done>Job posting form component is created and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Job Posting Form into Dashboard</name>
  <files>apps/web/app/dashboard/page.tsx</files>
  <read_first>
    - apps/web/app/dashboard/page.tsx
    - apps/web/components/dashboard/JobDashboard.tsx
  </read_first>
  <action>
    Update `apps/web/app/dashboard/page.tsx` to include the `JobPostingForm` component.
    Ensure the dashboard fetches the user's posted jobs on mount (GET `/api/jobs`).
    Pass a callback to `JobPostingForm` to refresh the job list after a successful post.
    Render the `JobDashboard` component (from Phase 6) to display the list of jobs.
  </action>
  <verify>
    <automated>grep -c "JobPostingForm" apps/web/app/dashboard/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/dashboard/page.tsx` imports and renders `JobPostingForm`
    - `apps/web/app/dashboard/page.tsx` fetches jobs and passes them to `JobDashboard`
    - `apps/web/app/dashboard/page.tsx` refreshes the job list after a new job is posted
  </acceptance_criteria>
  <done>Dashboard page includes the job posting form and displays the user's jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted job posting data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | Job Posting API | mitigate | Ensure the user is authenticated and has the CLIENT role (handled by API) |
| T-07-02 | Tampering | Job Posting API | mitigate | Validate form data against expected types and categories (handled by API) |
</threat_model>

<verification>
- `npm run typecheck` passes
- `grep -c "fetch.*api/jobs" apps/web/components/dashboard/JobPostingForm.tsx` returns > 0
- `grep -c "JobPostingForm" apps/web/app/dashboard/page.tsx` returns > 0
</verification>

<success_criteria>
- Client can fill out and submit the job posting form.
- New job appears on the dashboard with status PENDING.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-client-job-posting-dashboard/07-01-SUMMARY.md`
</output>
