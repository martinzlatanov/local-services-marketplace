---
phase: 13
plan: "01"
slug: provider-client-identity
title: "Foundation — Types, Schema, Migration"
wave: 1
depends_on: []
requirements: [IDENTITY-05]
decisions: [D-08, D-10]
status: draft
created: 2026-05-14
---

# Plan 13-01 — Foundation: Types, Schema, Migration

## Goal

Add the `PublicUserDto` interface to the shared type system and extend the `users` table with nullable `name` and `avatarUrl` columns via a Drizzle migration, then push the schema to the live Neon database. All downstream plans (13-02, 13-03, 13-04) depend on this being complete and pushed before execution.

## Must-Haves

- `PublicUserDto` is exported from `packages/types/src/index.ts` with exactly these fields: `id: string`, `email: string`, `name: string | null`, `avatarUrl: string | null`, `role: Role`, `createdAt: string`. `passwordHash` is NOT included.
- `PublicUserDto` is also present in `apps/web/lib/types.ts` (full local copy — not auto-synced from packages/types).
- `apps/web/lib/db/schema.ts` users table has `name: varchar("name", { length: 100 })` and `avatarUrl: text("avatar_url")` — no `.notNull()`, no `.default()`.
- `apps/web/drizzle/0002_add_user_identity.sql` exists with two `ALTER TABLE "users" ADD COLUMN IF NOT EXISTS` statements.
- `npx drizzle-kit push` runs successfully (exit 0) — columns confirmed present in Neon DB.
- `AuthUserDto` is unchanged. `JobDto` is unchanged.

## Truths

- D-08: Migration filename is `0002_add_user_identity.sql`. Directory currently has `0000_*` and `0001_*` files; next prefix is `0002_`.
- D-10: `PublicUserDto` shape is locked. Do not add fields. Do not reuse `AuthUserDto` — they serve different purposes.
- `apps/web/lib/types.ts` is a full local copy of types, NOT a wildcard re-export of `@local/types`. Both files must be updated manually.
- Both new columns are nullable, no backfill, no default — existing rows are unaffected.

---

## Tasks

<task id="T01-01">
<title>Add PublicUserDto to packages/types/src/index.ts</title>
<wave>1</wave>

<read_first>
- `packages/types/src/index.ts` — full file; locate AuthUserDto (lines ~96-101) and insert after it
</read_first>

<action>
Append the following interface after the `AuthUserDto` block in `packages/types/src/index.ts`:

```typescript
export interface PublicUserDto {
  id: string
  email: string
  name: string | null
  avatarUrl: string | null
  role: Role
  createdAt: string
}
```

Do NOT modify `AuthUserDto`. Do NOT modify `JobDto`. The `Role` enum is already defined in the same file — no new imports needed.
</action>

<acceptance_criteria>
- `grep -n "PublicUserDto" packages/types/src/index.ts` returns at least one match
- `grep -n "passwordHash" packages/types/src/index.ts` does NOT appear in the PublicUserDto block
- `grep -n "AuthUserDto" packages/types/src/index.ts` still matches (unchanged)
</acceptance_criteria>
</task>

<task id="T01-02">
<title>Add PublicUserDto to apps/web/lib/types.ts</title>
<wave>1</wave>
<depends_on>T01-01</depends_on>

<read_first>
- `apps/web/lib/types.ts` — full file; this is a full local copy of type definitions, NOT a re-export. Locate the AuthUserDto block and insert after it.
</read_first>

<action>
Append the following interface after the `AuthUserDto` block in `apps/web/lib/types.ts`:

```typescript
export interface PublicUserDto {
  id: string
  email: string
  name: string | null
  avatarUrl: string | null
  role: Role
  createdAt: string
}
```

Match the exact formatting style of the surrounding file (no trailing comma, same spacing). Do NOT modify any other interfaces.
</action>

<acceptance_criteria>
- `grep -n "PublicUserDto" apps/web/lib/types.ts` returns at least one match
- `grep -n "passwordHash" apps/web/lib/types.ts` does NOT appear in the PublicUserDto block
</acceptance_criteria>
</task>

<task id="T01-03">
<title>Add name and avatarUrl columns to schema.ts</title>
<wave>1</wave>

<read_first>
- `apps/web/lib/db/schema.ts` — full file; locate the users pgTable definition (lines ~4-10)
</read_first>

<action>
Inside the `users` pgTable call in `apps/web/lib/db/schema.ts`, append two new column declarations after `createdAt`:

```typescript
  name: varchar("name", { length: 100 }),
  avatarUrl: text("avatar_url"),
```

No `.notNull()` and no `.default(...)` on either — nullable by Drizzle convention. Match the indentation of the existing column declarations. Do NOT modify any other table definitions.
</action>

<acceptance_criteria>
- `grep -n "avatarUrl" apps/web/lib/db/schema.ts` returns a match inside the users table
- `grep -n "name:" apps/web/lib/db/schema.ts` returns a match with `varchar("name", { length: 100 })`
- `grep -n "notNull" apps/web/lib/db/schema.ts` — the new lines do NOT appear in this output
</acceptance_criteria>
</task>

<task id="T01-04">
<title>Write migration 0002_add_user_identity.sql</title>
<wave>1</wave>

<read_first>
- `apps/web/drizzle/` — list to confirm current files and prefix
- `apps/web/drizzle/0001_lean_juggernaut.sql` — reference for SQL format and file header style
</read_first>

<action>
Create `apps/web/drizzle/0002_add_user_identity.sql` with the following content:

```sql
-- 0002_add_user_identity.sql
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "name" VARCHAR(100);
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "avatar_url" TEXT;
```

Both columns are nullable with no default — consistent with D-08. Use `IF NOT EXISTS` to make the migration idempotent.
</action>

<acceptance_criteria>
- `test -f apps/web/drizzle/0002_add_user_identity.sql` exits 0
- `grep "ADD COLUMN IF NOT EXISTS" apps/web/drizzle/0002_add_user_identity.sql | wc -l` outputs `2`
- `grep "NOT NULL" apps/web/drizzle/0002_add_user_identity.sql | wc -l` outputs `0`
</acceptance_criteria>
</task>

<task id="T01-05" blocking="true">
<title>[BLOCKING] Run drizzle-kit push</title>
<wave>1</wave>
<depends_on>T01-03, T01-04</depends_on>

<read_first>
- `apps/web/package.json` — verify the push script or drizzle-kit CLI availability
- `apps/web/drizzle.config.ts` — confirm config path and schema path
</read_first>

<action>
Run the Drizzle schema push from the apps/web directory:

```bash
cd apps/web && npx drizzle-kit push
```

This pushes the updated schema.ts to the live Neon database, applying the migration. Confirm the command exits 0.

**If the push fails:** Check the DATABASE_URL environment variable is set, then re-run. Do NOT proceed to Plan 13-02 until this exits 0.
</action>

<acceptance_criteria>
- Command exits 0
- Manual verification: `psql $DATABASE_URL -c "\d users"` shows both `name` (character varying, nullable) and `avatar_url` (text, nullable) columns
</acceptance_criteria>
</task>
