---
phase: 05-real-time-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/api/jobs/accept.ts", "prisma/schema.prisma"]
autonomous: true
requirements: ["ACCEPT-01", "ACCEPT-02", "ACCEPT-03"]
user_setup: []
must_haves:
  truths:
    - "A provider can atomically accept a job."
    - "Concurrent acceptances resolve to one winner."
    - "Accepted jobs are removed from the PENDING list."
  artifacts:
    - path: "src/app/api/jobs/accept.ts"
      provides: "Job acceptance endpoint"
    - path: "prisma/schema.prisma"
      provides: "Job version field for optimistic locking"
  key_links:
    - from: "src/app/api/jobs/accept.ts"
      to: "prisma.schema.jobs"
      via: "Database query"
---

<objective>
Implement the `/jobs/:id/accept` endpoint with optimistic locking to ensure atomic job acceptance.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add version field to Job model</name>
  <files>prisma/schema.prisma</files>
  <action>Add a `version` field to the Job model for optimistic locking.</action>
  <verify>Run `prisma migrate` and confirm the schema update.</verify>
  <done>The Job model includes a `version` field.</done>
</task>

<task type="auto">
  <name>Task 2: Create job acceptance endpoint</name>
  <files>src/app/api/jobs/accept.ts</files>
  <action>Create a POST `/jobs/:id/accept` endpoint that increments the job version and assigns it to the provider.</action>
  <verify>Send concurrent POST requests and confirm one succeeds while others return HTTP 409.</verify>
  <done>The endpoint handles job acceptance atomically.</done>
</task>
</tasks>

<success_criteria>
- The `/jobs/:id/accept` endpoint updates the job version and assigns it atomically.
- Concurrent requests return HTTP 409 for losers.
- Accepted jobs are removed from the PENDING list.
</success_criteria>