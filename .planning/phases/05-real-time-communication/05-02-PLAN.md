---
phase: 05-real-time-communication
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["src/app/api/jobs/list.ts", "src/app/api/jobs/accept.ts"]
autonomous: true
requirements: ["ACCEPT-04", "ACCEPT-05"]
user_setup: []
must_haves:
  truths:
    - "Accepted jobs are removed from the PENDING list."
    - "The job DTO includes the updated version."
  artifacts:
    - path: "src/app/api/jobs/list.ts"
      provides: "Job listing endpoint"
    - path: "src/app/api/jobs/accept.ts"
      provides: "Job acceptance logic"
  key_links:
    - from: "src/app/api/jobs/list.ts"
      to: "src/app/api/jobs/accept.ts"
      via: "Shared job visibility logic"
---

<objective>
Ensure accepted jobs are removed from the PENDING list and the job DTO includes the updated version.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update job listing endpoint</name>
  <files>src/app/api/jobs/list.ts</files>
  <action>Modify the `/jobs` endpoint to exclude accepted jobs from the PENDING list.</action>
  <verify>Send a GET request to `/jobs` and confirm accepted jobs are excluded.</verify>
  <done>Accepted jobs no longer appear in the PENDING list.</done>
</task>

<task type="auto">
  <name>Task 2: Include updated version in job DTO</name>
  <files>src/app/api/jobs/accept.ts</files>
  <action>Ensure the HTTP 200 response from `/jobs/:id/accept` includes the updated job DTO with the incremented version.</action>
  <verify>Send a POST request to `/jobs/:id/accept` and confirm the response includes the updated DTO.</verify>
  <done>The job DTO includes the updated version.</done>
</task>
</tasks>

<success_criteria>
- Accepted jobs are excluded from the PENDING list.
- The job DTO includes the updated version in the HTTP 200 response.
</success_criteria>