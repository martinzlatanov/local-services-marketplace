# NPM workspace configuration for Vercel
# Tell npm to use the workspace packages instead of registry
@local:registry=https://registry.npmjs.org/
link-workspace-packages=true
save-workspace-protocols=false
